{
    "title": "MongoDB Import",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "mongoUrl": {
            "title": "Mongo connection URL",
            "type": "string",
            "description": "Connection URL to MongoDB, see [Connection String URI Format](https://docs.mongodb.com/manual/reference/connection-string/).",
            "editor": "textfield",
            "isSecret": true
        },
        "datasetId": {
            "title": "Dataset ID",
            "type": "string",
            "description": "Dataset ID from which data will be imported.",
            "editor": "textfield"
        },
        "collection": {
            "title": "Collection",
            "type": "string",
            "description": "Name of the collection to store the scraped data.",
            "editor": "textfield",
            "default": "results",
            "prefill": "results"
        },
        "uniqueKeys": {
            "title": "Unique attributes to identify a document",
            "type": "array",
            "description": "Unique keys for object, if you specified unique keys, act try to find object with this attributes in DB and update it.",
            "editor": "json"
        }
    },
    "required": ["mongoUrl", "datasetId", "collection"]
}
